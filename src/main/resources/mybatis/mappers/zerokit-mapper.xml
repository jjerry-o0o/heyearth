<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="zerokit.ZerokitDAO">

<select id="zerokit" resultType ="zerokitdto" parameterType="int">
	select * from zerokit where k_code=#{k_code};
</select>

	<select id="zerokitlist" resultType="zerokitdto">
		select * from zerokit;
	</select>


</mapper>