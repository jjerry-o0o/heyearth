package paging;

import org.checkerframework.checker.units.qual.m;

public class PaginationInfo {
	/*
	 * Criteria cri; //계산에 필요한 파라미터들이 담긴 클래스
	 * 
	 * int totalCount; //게시판 전체 데이터 개수 int displayPageNum = 5; //게시판 화면에서 한번에 보여질
	 * 페이지 번호 개수 int startPage; //화면 시작번호 int endPage; //화면 끝번호 boolean prev; //페이징
	 * 이전버튼 활성화여부 boolean next; //페이징 다음버턴 활성화여부
	 * 
	 * public int getTotalCount() { return totalCount; }
	 * 
	 * public void setTotalCount(int totalCount) { this.totalCount = totalCount;
	 * 
	 * pagingData(); }
	 * 
	 * public void pagingData() { //endpage = (현재 페이지 번호/화면에 보여질 페이지 번호 개수)*화면에 보여질
	 * 페이지 번호 개수 endPage =
	 * (int)(Math.ceil(cri.getPage()/(double)displayPageNum)*displayPageNum);
	 * 
	 * //startpage=(끝 페이지 번호 - 화면에 보여질 페이지 번호 개수) +1 startPage =
	 * (endPage-displayPageNum)+1;
	 * 
	 * //마지막페이지 번호 = 총 게시글 수 / 한페이지당 보여줄 게시글의 개수 int tempEndPage =
	 * (int)(Math.ceil(totalCount/(double)cri.getPerPageNum()));
	 * if(endPage>tempEndPage) { endPage=tempEndPage; }
	 * 
	 * //이전버튼 생성여부 = 시작페이지 번호가 1이면 false, 아니면 true prev = startPage ==1?false:true;
	 * 
	 * //다음버튼 생성여부 = 끝페이지 번호 * 한페이지당 보여줄 게시글의 개수가 총 게시글의 수보다 크거나 같으면 false, 아니면true
	 * next = endPage*cri.getPerPageNum() >= totalCount?false:true;
	 * 
	 * }
	 * 
	 * public Criteria getCri() { return cri; }
	 * 
	 * public void setCri(Criteria cri) { this.cri = cri; }
	 * 
	 * public int getDisplayPageNum() { return displayPageNum; }
	 * 
	 * public void setDisplayPageNum(int displayPageNum) { this.displayPageNum =
	 * displayPageNum; }
	 * 
	 * public int getStartPage() { return startPage; }
	 * 
	 * public void setStartPage(int startPage) { this.startPage = startPage; }
	 * 
	 * public int getEndPage() { return endPage; }
	 * 
	 * public void setEndPage(int endPage) { this.endPage = endPage; }
	 * 
	 * public boolean isPrev() { return prev; }
	 * 
	 * public void setPrev(boolean prev) { this.prev = prev; }
	 * 
	 * public boolean isNext() { return next; }
	 * 
	 * public void setNext(boolean next) { this.next = next; }
	 * 
	 * @Override public String toString() { return "PaginationInfo [cri=" + cri +
	 * ", totalCount=" + totalCount + ", displayPageNum=" + displayPageNum +
	 * ", startPage=" + startPage + ", endPage=" + endPage + ", prev=" + prev +
	 * ", next=" + next + "]"; }
	 */
	
	
	
	
}
